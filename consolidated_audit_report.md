# Daily Skill Security Audit Report - 2026-04-29

## Executive Summary
**Status:** ⚠️ **ATTENTION REQUIRED**

A security audit was performed on all installed OpenClaw skills in user and system directories. 

- **User Skills Path:** `~/.openclaw/skills`
- **System Skills Path:** `/usr/lib/node_modules/openclaw/skills`

The audit identified several critical and high-risk patterns. While many are legitimate configuration paths or self-references within the `skill-scanner` itself, several require manual verification.

---

## User Skills Audit Summary
**Path:** `/home/ubuntu/.openclaw/skills`
**Verdict:** REJECT (14 Critical Findings)

### Key Findings
1. **Self-Detections:** `skill-scanner` flagged itself for containing patterns related to `crypto_miner`, `reverse_shell`, and `base64_decode_exec`. These are expected as they are part of its detection signatures.
2. **Credential Paths:** Multiple skills (`gmail`, `gog-myclaw`, `api-gateway`, `stripe-api`, `whatsapp-business`) were flagged for referencing `~/.config` or `Bearer` tokens in their documentation (`SKILL.md`).
3. **External POST:** `gog-myclaw` flagged for `http_post_external` to `oauth2.googleapis.com`. This is expected for OAuth token exchange.
4. **Dynamic Execution:** `trevor-methodology` flagged for an `exec()` call in a JS template used for color conversion.

---

## System Skills Audit Summary
**Path:** `/usr/lib/node_modules/openclaw/skills`
**Verdict:** REJECT (19 Critical Findings)

### Key Findings
1. **Credential Paths:** Extensive flagging of configuration and token paths in documentation (`SKILL.md`) for skills like `bear-notes`, `himalaya`, `notion`, and `1password`.
2. **Config Locations:** Skills such as `eightctl`, `camsnap`, and `spotify-player` were flagged for documentation referencing `~/.config` directories.

---

## Recommendations
1. **Verify `trevor-methodology` JS:** Manually inspect `pipeline/docx-js-template.js` to ensure the `exec()` call is safe and doesn't handle unsanitized user input.
2. **Audit Config Access:** Ensure that skills accessing `~/.config` are only doing so for their own legitimate configuration and not attempting to exfiltrate other service tokens.
3. **Ignore Documentation Flags:** Findings inside `SKILL.md` files that merely describe setup procedures (e.g., "save your token to ~/.config/...") can generally be considered low-risk documentation references rather than active malicious code.

---
*Report generated by Trevor (Threat Research and Evaluation Virtual Operations Resource)*
