# Daily Skill Security Audit — 2026-05-06 00:00 UTC

**Skills scanned:** 111 total across 6 directories

## Summary

| Verdict | Count |
|---------|-------|
| **APPROVED** | 90 |
| **CAUTION** | 2 |
| **REJECT** | 19 |
| **ERROR** | 0 |

### By Directory

**/home/ubuntu/.openclaw/skills**
- `answeroverflow`: ✅ Approved - No critical or high-severity issues detected
- `api-gateway`: 🚫 REJECT - Found 1 critical issue(s): credential_paths (1 critical findings)
- `claude-code`: ✅ Approved - No critical or high-severity issues detected
- `find-skills`: ✅ Approved - No critical or high-severity issues detected
- `gmail`: 🚫 REJECT - Found 1 critical issue(s): credential_paths (1 critical findings)
- `gog-myclaw`: 🚫 REJECT - Found 2 critical issue(s): credential_paths (2 critical findings)
- `huggingface-hub`: ✅ Approved - No critical or high-severity issues detected
- `humanizer`: ✅ Approved - No critical or high-severity issues detected
- `maps-new`: ✅ Approved - No critical or high-severity issues detected
- `nano-pdf`: ✅ Approved - No critical or high-severity issues detected
- `network-analysis`: ✅ Approved - No critical or high-severity issues detected
- `ocr-docu`: ✅ Approved - No critical or high-severity issues detected
- `self-improving-agent`: ✅ Approved - No critical or high-severity issues detected
- `stock-market-pro`: ✅ Approved - No critical or high-severity issues detected
- `stripe-api`: 🚫 REJECT - Found 1 critical issue(s): credential_paths (1 critical findings)
- `trevor-methodology`: ⚠️ CAUTION - Found 1 high-severity issue(s): eval_exec (1 high findings)
- `video-frames`: ✅ Approved - No critical or high-severity issues detected
- `video-translation`: 🚫 REJECT - Found 1 critical issue(s): credential_paths (1 critical findings)
- `wacli`: ✅ Approved - No critical or high-severity issues detected
- `web-searchplus`: ✅ Approved - No critical or high-severity issues detected
- `whatsapp-business`: 🚫 REJECT - Found 1 critical issue(s): credential_paths (1 critical findings)
- `xurl`: ✅ Approved - No critical or high-severity issues detected
- `youtube-content`: ✅ Approved - No critical or high-severity issues detected

**/home/ubuntu/.openclaw/workspace/skills**
- `agent-intelligence-network-scan`: ✅ Approved - No critical or high-severity issues detected
- `agentmail`: ✅ Approved - No critical or high-severity issues detected
- `baoyu-translate`: ✅ Approved - No critical or high-severity issues detected
- `bluf-report`: ✅ Approved - No critical or high-severity issues detected
- `chartgen-ai`: 🚫 REJECT - Found 5 critical issue(s): credential_paths (5 critical findings)
- `content-generation`: ✅ Approved - No critical or high-severity issues detected
- `content-marketing`: ✅ Approved - No critical or high-severity issues detected
- `cross-poster`: ✅ Approved - No critical or high-severity issues detected
- `daily-intel-brief`: ⚠️ CAUTION - Found 1 high-severity issue(s): bulk_env_access (1 high findings)
- `data-analysis`: ✅ Approved - No critical or high-severity issues detected
- `data-visualization-studio`: ✅ Approved - No critical or high-severity issues detected
- `genviral`: 🚫 REJECT - Found 2 critical issue(s): credential_paths (2 critical findings)
- `geospatial-osint`: ✅ Approved - No critical or high-severity issues detected
- `indicators-and-warnings`: ✅ Approved - No critical or high-severity issues detected
- `landing-page-generator`: ✅ Approved - No critical or high-severity issues detected
- `landing-page-roast`: ✅ Approved - No critical or high-severity issues detected
- `mermaid`: ✅ Approved - No critical or high-severity issues detected
- `newsletter`: ✅ Approved - No critical or high-severity issues detected
- `newsletter-creation-curation`: ✅ Approved - No critical or high-severity issues detected
- `pdf-report`: ✅ Approved - No critical or high-severity issues detected
- `polymarket-trader`: ✅ Approved - No critical or high-severity issues detected
- `quick-translation`: ✅ Approved - No critical or high-severity issues detected
- `renderers`: ✅ Approved - No critical or high-severity issues detected
- `sat-toolkit`: ✅ Approved - No critical or high-severity issues detected
- `skill-stripe-monitor`: ✅ Approved - No critical or high-severity issues detected
- `social-media-agent`: ✅ Approved - No critical or high-severity issues detected
- `social-media-scheduler`: ✅ Approved - No critical or high-severity issues detected
- `social-pack`: ✅ Approved - No critical or high-severity issues detected
- `social-post`: 🚫 REJECT - Found 36 critical issue(s): credential_paths (36 critical findings)
- `social-poster`: ✅ Approved - No critical or high-severity issues detected
- `source-evaluation`: ✅ Approved - No critical or high-severity issues detected
- `visual_production`: ✅ Approved - No critical or high-severity issues detected

**/usr/lib/node_modules/openclaw/skills**
- `1password`: 🚫 REJECT - Found 1 critical issue(s): credential_paths (1 critical findings)
- `apple-notes`: ✅ Approved - No critical or high-severity issues detected
- `apple-reminders`: ✅ Approved - No critical or high-severity issues detected
- `bear-notes`: 🚫 REJECT - Found 7 critical issue(s): credential_paths (7 critical findings)
- `blogwatcher`: ✅ Approved - No critical or high-severity issues detected
- `blucli`: ✅ Approved - No critical or high-severity issues detected
- `bluebubbles`: ✅ Approved - No critical or high-severity issues detected
- `camsnap`: 🚫 REJECT - Found 1 critical issue(s): credential_paths (1 critical findings)
- `canvas`: ✅ Approved - No critical or high-severity issues detected
- `clawhub`: ✅ Approved - No critical or high-severity issues detected
- `coding-agent`: ✅ Approved - No critical or high-severity issues detected
- `discord`: ✅ Approved - No critical or high-severity issues detected
- `eightctl`: 🚫 REJECT - Found 1 critical issue(s): credential_paths (1 critical findings)
- `gemini`: ✅ Approved - No critical or high-severity issues detected
- `gh-issues`: ✅ Approved - No critical or high-severity issues detected
- `gifgrep`: ✅ Approved - No critical or high-severity issues detected
- `github`: ✅ Approved - No critical or high-severity issues detected
- `gog`: ✅ Approved - No critical or high-severity issues detected
- `goplaces`: ✅ Approved - No critical or high-severity issues detected
- `healthcheck`: ✅ Approved - No critical or high-severity issues detected
- `himalaya`: 🚫 REJECT - Found 3 critical issue(s): credential_paths (3 critical findings)
- `imsg`: ✅ Approved - No critical or high-severity issues detected
- `mcporter`: ✅ Approved - No critical or high-severity issues detected
- `model-usage`: 🚫 REJECT - Found 1 critical issue(s): credential_paths (1 critical findings)
- `nano-pdf`: ✅ Approved - No critical or high-severity issues detected
- `node-connect`: ✅ Approved - No critical or high-severity issues detected
- `notion`: 🚫 REJECT - Found 3 critical issue(s): credential_paths (3 critical findings)
- `obsidian`: ✅ Approved - No critical or high-severity issues detected
- `openai-whisper`: ✅ Approved - No critical or high-severity issues detected
- `openai-whisper-api`: ✅ Approved - No critical or high-severity issues detected
- `openhue`: ✅ Approved - No critical or high-severity issues detected
- `oracle`: 🚫 REJECT - Found 1 critical issue(s): credential_paths (1 critical findings)
- `ordercli`: ✅ Approved - No critical or high-severity issues detected
- `peekaboo`: ✅ Approved - No critical or high-severity issues detected
- `sag`: ✅ Approved - No critical or high-severity issues detected
- `session-logs`: ✅ Approved - No critical or high-severity issues detected
- `sherpa-onnx-tts`: ✅ Approved - No critical or high-severity issues detected
- `skill-creator`: ✅ Approved - No critical or high-severity issues detected
- `slack`: ✅ Approved - No critical or high-severity issues detected
- `songsee`: ✅ Approved - No critical or high-severity issues detected
- `sonoscli`: ✅ Approved - No critical or high-severity issues detected
- `spotify-player`: 🚫 REJECT - Found 1 critical issue(s): credential_paths (1 critical findings)
- `summarize`: ✅ Approved - No critical or high-severity issues detected
- `taskflow`: ✅ Approved - No critical or high-severity issues detected
- `taskflow-inbox-triage`: ✅ Approved - No critical or high-severity issues detected
- `things-mac`: ✅ Approved - No critical or high-severity issues detected
- `tmux`: ✅ Approved - No critical or high-severity issues detected
- `trello`: ✅ Approved - No critical or high-severity issues detected
- `video-frames`: ✅ Approved - No critical or high-severity issues detected
- `voice-call`: ✅ Approved - No critical or high-severity issues detected
- `wacli`: ✅ Approved - No critical or high-severity issues detected
- `weather`: ✅ Approved - No critical or high-severity issues detected
- `xurl`: ✅ Approved - No critical or high-severity issues detected

**/usr/lib/node_modules/openclaw/dist/extensions/acpx/skills**
- `acp-router`: 🚫 REJECT - Found 1 critical issue(s): credential_paths (1 critical findings)

**/usr/lib/node_modules/openclaw/dist/extensions/browser/skills**
- `browser-automation`: ✅ Approved - No critical or high-severity issues detected

**/usr/lib/node_modules/openclaw/dist/extensions/tavily/skills**
- `tavily`: ✅ Approved - No critical or high-severity issues detected

## Detailed Findings (Non-Approved Only)

### CRITICAL Severity

- **api-gateway** — `credential_paths` in `SKILL.md:539`
  - Accesses sensitive credential locations
  - _Recommendation:_ REJECT unless explicitly justified
  - `'Authorization': `Bearer ${process.env.MATON_API_KEY}``

- **gmail** — `credential_paths` in `SKILL.md:267`
  - Accesses sensitive credential locations
  - _Recommendation:_ REJECT unless explicitly justified
  - `'Authorization': `Bearer ${process.env.MATON_API_KEY}``

- **gog-myclaw** — `credential_paths` in `SKILL.md:23`
  - Accesses sensitive credential locations
  - _Recommendation:_ REJECT unless explicitly justified
  - `3. Once they provide the `credentials.json` content, save it to `~/.config/gogcli/credentials.json`.`

- **gog-myclaw** — `credential_paths` in `SKILL.md:24`
  - Accesses sensitive credential locations
  - _Recommendation:_ REJECT unless explicitly justified
  - `4. Run: `gog auth credentials set ~/.config/gogcli/credentials.json``

- **stripe-api** — `credential_paths` in `SKILL.md:778`
  - Accesses sensitive credential locations
  - _Recommendation:_ REJECT unless explicitly justified
  - `'Authorization': `Bearer ${process.env.MATON_API_KEY}``

- **video-translation** — `credential_paths` in `SKILL.md:90`
  - Accesses sensitive credential locations
  - _Recommendation:_ REJECT unless explicitly justified
  - `- `NOIZ_API_KEY` configured for the Noiz backend. If it is not set, first guide the user to get an API key from `https://developers.noiz.ai/api-keys`. After the user provides the key, ask whether they`

- **whatsapp-business** — `credential_paths` in `SKILL.md:494`
  - Accesses sensitive credential locations
  - _Recommendation:_ REJECT unless explicitly justified
  - `'Authorization': `Bearer ${process.env.MATON_API_KEY}`,`

- **chartgen-ai** — `credential_paths` in `tools/chartgen_api.js:25`
  - Accesses sensitive credential locations
  - _Recommendation:_ REJECT unless explicitly justified
  - `const BASE_URL = process.env.CHARTGEN_API_URL || "https://chartgen.ai";`

- **chartgen-ai** — `credential_paths` in `tools/chartgen_api.js:40`
  - Accesses sensitive credential locations
  - _Recommendation:_ REJECT unless explicitly justified
  - `if (process.env.CHARTGEN_API_KEY) return process.env.CHARTGEN_API_KEY;`

- **chartgen-ai** — `credential_paths` in `tools/chartgen_api.js:44`
  - Accesses sensitive credential locations
  - _Recommendation:_ REJECT unless explicitly justified
  - `process.env.OPENCLAW_STATE_DIR`

- **chartgen-ai** — `credential_paths` in `tools/chartgen_api.js:45`
  - Accesses sensitive credential locations
  - _Recommendation:_ REJECT unless explicitly justified
  - `? path.join(process.env.OPENCLAW_STATE_DIR, "skills", "chartgen", "config.json")`

- **chartgen-ai** — `credential_paths` in `tools/chartgen_api.js:74`
  - Accesses sensitive credential locations
  - _Recommendation:_ REJECT unless explicitly justified
  - `const stateDir = process.env.OPENCLAW_STATE_DIR;`

- **genviral** — `credential_paths` in `scripts/genviral.sh:192`
  - Accesses sensitive credential locations
  - _Recommendation:_ REJECT unless explicitly justified
  - `[[ -f "${HOME}/.config/env/global.env" ]] && source "${HOME}/.config/env/global.env" 2>/dev/null || true`

- **genviral** — `credential_paths` in `scripts/genviral.sh:219`
  - Accesses sensitive credential locations
  - _Recommendation:_ REJECT unless explicitly justified
  - `die "GENVIRAL_API_KEY is not set.\n  Set it via: export GENVIRAL_API_KEY=\"your_public_id.your_secret\"\n  Or add to ~/.config/env/global.env"`

- **social-post** — `credential_paths` in `CHANGELOG.md:42`
  - Accesses sensitive credential locations
  - _Recommendation:_ REJECT unless explicitly justified
  - `- Support for custom credential prefixes in `.env` file`

- **social-post** — `credential_paths` in `CHANGELOG.md:143`
  - Accesses sensitive credential locations
  - _Recommendation:_ REJECT unless explicitly justified
  - `- ❌ Fixed `.env` file parsing error (quoted mnemonic)`

- **social-post** — `credential_paths` in `CHANGELOG.md:175`
  - Accesses sensitive credential locations
  - _Recommendation:_ REJECT unless explicitly justified
  - `- Automatic credential loading from `.env` and Farcaster credentials file`

- **social-post** — `credential_paths` in `README.md:44`
  - Accesses sensitive credential locations
  - _Recommendation:_ REJECT unless explicitly justified
  - `| **X/Twitter** | Pay-per-use (consumption-based) | `~/.openclaw/.env` | 5-10 min |`

- **social-post** — `credential_paths` in `README.md:91`
  - Accesses sensitive credential locations
  - _Recommendation:_ REJECT unless explicitly justified
  - `**Step 3: Add to .env file**`

- **social-post** — `credential_paths` in `README.md:92`
  - Accesses sensitive credential locations
  - _Recommendation:_ REJECT unless explicitly justified
  - `Location: `/home/phan_harry/.openclaw/.env``

- **social-post** — `credential_paths` in `README.md:180`
  - Accesses sensitive credential locations
  - _Recommendation:_ REJECT unless explicitly justified
  - `grep "^X_CONSUMER_KEY" ~/.openclaw/.env`

- **social-post** — `credential_paths` in `README.md:308`
  - Accesses sensitive credential locations
  - _Recommendation:_ REJECT unless explicitly justified
  - `1. Check `.env` file exists: `ls -la ~/.openclaw/.env``

- **social-post** — `credential_paths` in `README.md:311`
  - Accesses sensitive credential locations
  - _Recommendation:_ REJECT unless explicitly justified
  - `grep "^X_" ~/.openclaw/.env`

- **social-post** — `credential_paths` in `README.md:314`
  - Accesses sensitive credential locations
  - _Recommendation:_ REJECT unless explicitly justified
  - `4. Check file permissions: `chmod 600 ~/.openclaw/.env``

- **social-post** — `credential_paths` in `SKILL.md:43`
  - Accesses sensitive credential locations
  - _Recommendation:_ REJECT unless explicitly justified
  - `**Required credentials** (stored in `/home/phan_harry/.openclaw/.env`):`

- **social-post** — `credential_paths` in `SKILL.md:76`
  - Accesses sensitive credential locations
  - _Recommendation:_ REJECT unless explicitly justified
  - `4. **Add to .env file**`

- **social-post** — `credential_paths` in `SKILL.md:78`
  - Accesses sensitive credential locations
  - _Recommendation:_ REJECT unless explicitly justified
  - `echo "X_CONSUMER_KEY=xxx" >> ~/.openclaw/.env`

- **social-post** — `credential_paths` in `SKILL.md:79`
  - Accesses sensitive credential locations
  - _Recommendation:_ REJECT unless explicitly justified
  - `echo "X_CONSUMER_SECRET=xxx" >> ~/.openclaw/.env`

- **social-post** — `credential_paths` in `SKILL.md:80`
  - Accesses sensitive credential locations
  - _Recommendation:_ REJECT unless explicitly justified
  - `echo "X_ACCESS_TOKEN=xxx" >> ~/.openclaw/.env`

- **social-post** — `credential_paths` in `SKILL.md:81`
  - Accesses sensitive credential locations
  - _Recommendation:_ REJECT unless explicitly justified
  - `echo "X_ACCESS_TOKEN_SECRET=xxx" >> ~/.openclaw/.env`

- **social-post** — `credential_paths` in `SKILL.md:98`
  - Accesses sensitive credential locations
  - _Recommendation:_ REJECT unless explicitly justified
  - `echo "MYACCOUNT_API_KEY=xxx" >> ~/.openclaw/.env`

- **social-post** — `credential_paths` in `SKILL.md:99`
  - Accesses sensitive credential locations
  - _Recommendation:_ REJECT unless explicitly justified
  - `echo "MYACCOUNT_API_KEY_SECRET=xxx" >> ~/.openclaw/.env`

- **social-post** — `credential_paths` in `SKILL.md:100`
  - Accesses sensitive credential locations
  - _Recommendation:_ REJECT unless explicitly justified
  - `echo "MYACCOUNT_ACCESS_TOKEN=xxx" >> ~/.openclaw/.env`

- **social-post** — `credential_paths` in `SKILL.md:101`
  - Accesses sensitive credential locations
  - _Recommendation:_ REJECT unless explicitly justified
  - `echo "MYACCOUNT_ACCESS_TOKEN_SECRET=xxx" >> ~/.openclaw/.env`

- **social-post** — `credential_paths` in `SKILL.md:179`
  - Accesses sensitive credential locations
  - _Recommendation:_ REJECT unless explicitly justified
  - `- ⚠️ `.env` file should have `600` permissions (read/write owner only)`

- **social-post** — `credential_paths` in `SKILL.md:253`
  - Accesses sensitive credential locations
  - _Recommendation:_ REJECT unless explicitly justified
  - `- `--account <name>` - Twitter account to use (lowercase prefix from .env)`

- **social-post** — `credential_paths` in `SKILL.md:266`
  - Accesses sensitive credential locations
  - _Recommendation:_ REJECT unless explicitly justified
  - `- `--account <name>` - Twitter account to use (lowercase prefix from .env)`

- **social-post** — `credential_paths` in `SKILL.md:362`
  - Accesses sensitive credential locations
  - _Recommendation:_ REJECT unless explicitly justified
  - `- Twitter credentials in `.env` (X_CONSUMER_KEY, X_CONSUMER_SECRET, X_ACCESS_TOKEN, X_ACCESS_TOKEN_SECRET)`

- **social-post** — `credential_paths` in `lib/farcaster.sh:30`
  - Accesses sensitive credential locations
  - _Recommendation:_ REJECT unless explicitly justified
  - `const wallet = new Wallet(process.env.PRIVATE_KEY, baseProvider);`

- **social-post** — `credential_paths` in `lib/farcaster.sh:31`
  - Accesses sensitive credential locations
  - _Recommendation:_ REJECT unless explicitly justified
  - `const signerBytes = Buffer.from(process.env.SIGNER_PRIVATE_KEY, 'hex');`

- **social-post** — `credential_paths` in `lib/farcaster.sh:33`
  - Accesses sensitive credential locations
  - _Recommendation:_ REJECT unless explicitly justified
  - `const fid = parseInt(process.env.FID);`

- **social-post** — `credential_paths` in `lib/farcaster.sh:34`
  - Accesses sensitive credential locations
  - _Recommendation:_ REJECT unless explicitly justified
  - `const parentHashBytes = Buffer.from(process.env.PARENT_HASH.replace('0x', ''), 'hex');`

- **social-post** — `credential_paths` in `lib/farcaster.sh:117`
  - Accesses sensitive credential locations
  - _Recommendation:_ REJECT unless explicitly justified
  - `const wallet = new Wallet(process.env.PRIVATE_KEY, baseProvider);`

- **social-post** — `credential_paths` in `lib/farcaster.sh:118`
  - Accesses sensitive credential locations
  - _Recommendation:_ REJECT unless explicitly justified
  - `const signerBytes = Buffer.from(process.env.SIGNER_PRIVATE_KEY, 'hex');`

- **social-post** — `credential_paths` in `lib/farcaster.sh:120`
  - Accesses sensitive credential locations
  - _Recommendation:_ REJECT unless explicitly justified
  - `const fid = parseInt(process.env.FID);`

- **social-post** — `credential_paths` in `lib/farcaster.sh:121`
  - Accesses sensitive credential locations
  - _Recommendation:_ REJECT unless explicitly justified
  - `const imageUrl = process.env.IMAGE_URL;`

- **social-post** — `credential_paths` in `lib/farcaster.sh:131`
  - Accesses sensitive credential locations
  - _Recommendation:_ REJECT unless explicitly justified
  - `if (process.env.PARENT_HASH) {`

- **social-post** — `credential_paths` in `lib/farcaster.sh:132`
  - Accesses sensitive credential locations
  - _Recommendation:_ REJECT unless explicitly justified
  - `const parentHashBytes = Buffer.from(process.env.PARENT_HASH.replace('0x', ''), 'hex');`

- **social-post** — `credential_paths` in `lib/twitter.sh:8`
  - Accesses sensitive credential locations
  - _Recommendation:_ REJECT unless explicitly justified
  - `source /home/phan_harry/.openclaw/.env`

- **social-post** — `credential_paths` in `scripts/reply.sh:272`
  - Accesses sensitive credential locations
  - _Recommendation:_ REJECT unless explicitly justified
  - `source /home/phan_harry/.openclaw/.env`

- **1password** — `credential_paths` in `references/cli-examples.md:19`
  - Accesses sensitive credential locations
  - _Recommendation:_ REJECT unless explicitly justified
  - `- `op run --env-file="./.env" -- printenv DB_PASSWORD``

- **bear-notes** — `credential_paths` in `SKILL.md:33`
  - Accesses sensitive credential locations
  - _Recommendation:_ REJECT unless explicitly justified
  - `- For some operations (add-text, tags, open-note --selected), a Bear app token (stored in `~/.config/grizzly/token`)`

- **bear-notes** — `credential_paths` in `SKILL.md:40`
  - Accesses sensitive credential locations
  - _Recommendation:_ REJECT unless explicitly justified
  - `2. Save it: `echo "YOUR_TOKEN" > ~/.config/grizzly/token``

- **bear-notes** — `credential_paths` in `SKILL.md:60`
  - Accesses sensitive credential locations
  - _Recommendation:_ REJECT unless explicitly justified
  - `echo "Additional content" | grizzly add-text --id "NOTE_ID" --mode append --token-file ~/.config/grizzly/token`

- **bear-notes** — `credential_paths` in `SKILL.md:66`
  - Accesses sensitive credential locations
  - _Recommendation:_ REJECT unless explicitly justified
  - `grizzly tags --enable-callback --json --token-file ~/.config/grizzly/token`

- **bear-notes** — `credential_paths` in `SKILL.md:92`
  - Accesses sensitive credential locations
  - _Recommendation:_ REJECT unless explicitly justified
  - `4. `~/.config/grizzly/config.toml``

- **bear-notes** — `credential_paths` in `SKILL.md:94`
  - Accesses sensitive credential locations
  - _Recommendation:_ REJECT unless explicitly justified
  - `Example `~/.config/grizzly/config.toml`:`

- **bear-notes** — `credential_paths` in `SKILL.md:97`
  - Accesses sensitive credential locations
  - _Recommendation:_ REJECT unless explicitly justified
  - `token_file = "~/.config/grizzly/token"`

- **camsnap** — `credential_paths` in `SKILL.md:31`
  - Accesses sensitive credential locations
  - _Recommendation:_ REJECT unless explicitly justified
  - `- Config file: `~/.config/camsnap/config.yaml``

- **eightctl** — `credential_paths` in `SKILL.md:31`
  - Accesses sensitive credential locations
  - _Recommendation:_ REJECT unless explicitly justified
  - `- Config: `~/.config/eightctl/config.yaml``

- **himalaya** — `credential_paths` in `SKILL.md:37`
  - Accesses sensitive credential locations
  - _Recommendation:_ REJECT unless explicitly justified
  - `2. A configuration file at `~/.config/himalaya/config.toml``

- **himalaya** — `credential_paths` in `SKILL.md:48`
  - Accesses sensitive credential locations
  - _Recommendation:_ REJECT unless explicitly justified
  - `Or create `~/.config/himalaya/config.toml` manually:`

- **himalaya** — `credential_paths` in `references/configuration.md:3`
  - Accesses sensitive credential locations
  - _Recommendation:_ REJECT unless explicitly justified
  - `Configuration file location: `~/.config/himalaya/config.toml``

- **model-usage** — `credential_paths` in `references/codexbar-cli.md:32`
  - Accesses sensitive credential locations
  - _Recommendation:_ REJECT unless explicitly justified
  - `- Claude: ~/.config/claude/projects/**/\*.jsonl or ~/.claude/projects/**/\*.jsonl`

- **notion** — `credential_paths` in `SKILL.md:23`
  - Accesses sensitive credential locations
  - _Recommendation:_ REJECT unless explicitly justified
  - `mkdir -p ~/.config/notion`

- **notion** — `credential_paths` in `SKILL.md:24`
  - Accesses sensitive credential locations
  - _Recommendation:_ REJECT unless explicitly justified
  - `echo "ntn_your_key_here" > ~/.config/notion/api_key`

- **notion** — `credential_paths` in `SKILL.md:34`
  - Accesses sensitive credential locations
  - _Recommendation:_ REJECT unless explicitly justified
  - `NOTION_KEY=$(cat ~/.config/notion/api_key)`

- **oracle** — `credential_paths` in `SKILL.md:115`
  - Accesses sensitive credential locations
  - _Recommendation:_ REJECT unless explicitly justified
  - `- Don’t attach secrets by default (`.env`, key files, auth tokens). Redact aggressively; share only what’s required.`

- **spotify-player** — `credential_paths` in `SKILL.md:62`
  - Accesses sensitive credential locations
  - _Recommendation:_ REJECT unless explicitly justified
  - `- Config folder: `~/.config/spotify-player` (e.g., `app.toml`).`

- **acp-router** — `credential_paths` in `SKILL.md:128`
  - Accesses sensitive credential locations
  - _Recommendation:_ REJECT unless explicitly justified
  - `- `node -e "console.log(require(process.env.ACPX_PLUGIN_ROOT + '/package.json').dependencies.acpx)"``

### HIGH Severity

- **trevor-methodology** — `eval_exec` in `pipeline/docx-js-template.js:37`
  - Dynamic code execution - could run arbitrary code
  - _Recommendation:_ Verify input is sanitized, not user-controlled
  - `const result = /^#?([a-f\d]{2})([a-f\d]{2})([a-f\d]{2})$/i.exec(hex);`

- **daily-intel-brief** — `bulk_env_access` in `scripts/build_pdf.py:330`
  - Bulk access to all environment variables - likely exfiltration
  - _Recommendation:_ REJECT - review carefully for data theft
  - `env = dict(os.environ)`

### MEDIUM Severity

- **gog-myclaw** — `http_post_external` in `config/exchange.py:16`
  - HTTP POST to external endpoint - could exfiltrate data
  - _Recommendation:_ Verify destination URL is expected and documented
  - `response = requests.post('https://oauth2.googleapis.com/token', data=data)`

- **daily-intel-brief** — `env_scraping` in `scripts/analyze.py:35`
  - Reads environment variables - could access secrets
  - _Recommendation:_ Verify only expected env vars are read, not bulk scraping
  - `DEEPSEEK_BASE = os.environ.get("DEEPSEEK_BASE", "https://api.deepseek.com")`

- **daily-intel-brief** — `env_scraping` in `scripts/analyze.py:89`
  - Reads environment variables - could access secrets
  - _Recommendation:_ Verify only expected env vars are read, not bulk scraping
  - `api_key = os.environ.get("DEEPSEEK_API_KEY")`

- **daily-intel-brief** — `env_scraping` in `scripts/build_visuals.py:152`
  - Reads environment variables - could access secrets
  - _Recommendation:_ Verify only expected env vars are read, not bulk scraping
  - `token = os.environ.get("MAPBOX_TOKEN")`

- **daily-intel-brief** — `env_scraping` in `scripts/orchestrate.py:54`
  - Reads environment variables - could access secrets
  - _Recommendation:_ Verify only expected env vars are read, not bulk scraping
  - `missing = [k for k in REQUIRED_ENV if not os.environ.get(k)]`

- **daily-intel-brief** — `env_scraping` in `scripts/orchestrate.py:62`
  - Reads environment variables - could access secrets
  - _Recommendation:_ Verify only expected env vars are read, not bulk scraping
  - `if not os.environ.get(k):`

- **daily-intel-brief** — `env_scraping` in `scripts/orchestrate.py:332`
  - Reads environment variables - could access secrets
  - _Recommendation:_ Verify only expected env vars are read, not bulk scraping
  - `if os.environ.get("AGENTMAIL_API_KEY"):`

---

_Audit completed at 2026-05-06 00:00 UTC_
_Total skills: 111_
_Approved: 90 | Caution: 2 | Rejected: 19 | Errors: 0_