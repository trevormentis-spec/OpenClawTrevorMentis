# 2026-05-18 — Phase 1 Closure + Phase 2 Launch + Presentation Stack

## Durable Decisions

### Phase 1 Closed
- Scope gate (scope_check.py + scope.yaml) verified at 4/4 regression probes. Blocklist trimmed from 23→11 entries (removed 12 overreaching keywords). Blocklist = precision tool, not filter.
- Fabrication detector (analyst/fabrication_check.py) blocks briefs with unsourced contracts, prices, percentage ranges, dollar cost estimates. Hard rule: no named instruments without verification.
- Themes preflight (analyst/themes_preflight.py) validates all required themes have substantive coverage pre-delivery.
- Three guards (scope_check, fabrication_check, themes_preflight) are principal-review-only for changes.

### Phase 2 — Continuous Learning Loop Launched
- `analyst/planner.py` — state inspection → 5-15 tasks/cycle → queue at analyst/queue/
- `analyst/worker.py` — defaults to dry-run mode, --live flag required for execution
- 6 task types: entity_deepening, postdiction_sweep, source_freshness_scan, self_question_generation, framework_stress_test, cross_source_correlation
- Mexico pipeline converged: planner reads from `exports/mexico-news-scan-*.json`, not global incidents.json
- Worker live mode authorized. Daily budget: $20. First live cycle: $0.011 of $20.

### Presentation Stack — Authorized Builds
- JSON schema v1.0.0 at `docs/brief-schema-v1.md` — Sherman Kent enums, Admiralty grades, stable UUIDs, public API contract
- PDF renderer at `scripts/render_pdf.py` — weasyprint, branded HTML/CSS template, zero new dependencies. Bajío v3 round-trip tested (28KB, 5 pages).
- Audio renderer at `scripts/render_audio.py` — ElevenLabs integration, pronunciation dictionary (80+ entries). Dry-run verified. Blocked: ELEVENLABS_API_KEY not in environment (all Maton connections PENDING).

### Source Audit — Registry Status
- 83 sources in `sources-mexico.json` (expanded to 97 with 14 Spanish-language additions)
- ALL 83 are LISTED only — zero WIRED fetchers, zero USED citations
- Energy_infra has 22 LISTED sources — not a registry gap but a pipeline gap (collector doesn't read from Mexico registry)
- 7 programmatic sources proposed and parked: SESNSP (highest priority), ACLED, INEGI, GDELT, Banxico, DOF, OFAC

### Architecture Rules Codified
- Autonomy boundaries in ORCHESTRATION.md: self-detected, clean diagnosis, contained fix, regression-testable = autonomous. Principal review for architectural/guard/identity changes.
- Default-non-executing rule: new automated executors default to dry-run; explicit --live flag required.
- Preflight data rule: tracking data changes must confirm prior state is recoverable before commit.

### Key Technical Debts
1. Entity deepening task module scaffolds-only — needs actual Haiku/Opus call wired
2. Framework stress test module scaffolds-only — needs actual framework analysis call
3. Self_question_generation hardcodes 4 questions — needs LLM call for novel gaps
4. Cross_source_correlation dormant — needs multi-source articles in Mexico pipeline
5. ElevenLabs connection PENDING — audio blocked without API key
6. mermaid skill, data-charts-visualization, visual_production, landing-page skills all installed-unregistered in openclaw.json
7. Mapbox token (pk.) in .env but no active MCP server or skill activation
