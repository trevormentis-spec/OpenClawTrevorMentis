
## Collection Capability Expansion (2026-05-19)

### What changed
- Installed `openweb` (94 site API specs) and `reverse-api-engineer` (HAR→spec generator)
- Both MIT-licensed, both pinned as git submodules
- Prior collection: ~15 RSS feeds + HTTP DOM scraping
- Post-install: 94 structured API endpoints + custom spec generation pipeline

### Coverage estimate grade
Prior assessment: **PARTIALLY CONFIRMED** — RSS+DOM was accurate for what existed, but materially underestimated the availability of programmatic API access. Openweb alone adds 79 more structured data endpoints than prior collection had total sources (94 vs 15).

### Key findings
- ANTHROPIC_API_KEY present but invalid — blocks Claude-powered spec generation in RAE agent mode. HAR capture works independently.
- google-chrome binary not available — blocks browser-backed auth for sites requiring it. chromium-browser is a snap wrapper only.
- Test 1 (Wikipedia): PASS — sub-second JSON, 0 lines client code
- Test 2 (El Financiero HAR): PASS for capture (324 entries), FAIL for Claude spec gen (bad key)
- Test 3 (HackerNews): PASS — HTTP transport, no browser needed
- BigQuery pipeline: working (2 records in tasks/collection_records.jsonl)
- Decision: no public pushes to openweb repo without Roderick's approval
